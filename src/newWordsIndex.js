import React from 'react';
import ReactDOM from 'react-dom/client';
import NewWordsApp from './components/NewWordsApp.jsx';

const root = ReactDOM.createRoot(document.getElementById('react-root'));
root.render(
    <NewWordsApp />
);